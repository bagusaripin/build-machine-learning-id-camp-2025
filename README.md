# build-machine-learning-id-camp-2025
The dataset was analyzed using K-Means clustering (Elbow Method, PCA, Silhouette Score), then classified using Decision Tree and KNN, evaluated with a confusion matrix.
